export interface Event {
    title: string,
    describ: string,
    initDate: string,
    endDate: string,
    background?: string,
    _id: string,
    owner: string,
    isPrivate: boolean,
    isActive: boolean
}